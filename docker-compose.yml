services:
  dlna-ma-bridge:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dlna-ma-bridge
    network_mode: host
    environment:
      MA_TOKEN: "${MA_TOKEN:-}"
    volumes:
      - ./config.yaml:/config/config.yaml:ro
    restart: unless-stopped
